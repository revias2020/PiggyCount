# 小猪记账 · 升级日志

## 0.4.3

**版本：** `0.4.3+10`  
**相对：** `0.4.2+9`  
**日期：** 2026-08-28

### 截图自动：关联窗起算与删原补扫（ADR-064）

编辑另存新名并删原图时，旧「自首次检出固定 15s」关联窗易在慢编辑后丢原、静默取消。改为：**替换关联窗 15s 自最近一次稳定期满起算**；**候选总时限 2 分钟**硬切；原文件不在且无后继时 **删原补扫**（同目录 + 截图关键词 + ±15s）；取消统一通知「截图已取消，未入账」。

### 截图自动：非候选过滤（回收站误检）

删除/进回收站会触发 MediaStore，且 trash 编码名常仍含 `Screenshot`，曾被当成新截图开稳定期。现对 `IS_TRASHED`、path/name 含 trash、DISPLAY_NAME 含 `@delete` 做非候选过滤（短日志）；删原补扫同样跳过。

### 分享已收到进度：回源后起算（ADR-063）

热启分享时「已收到…」曾在 MainActivity 前台 hold，下拉栏常看不到，回源后已被盖成「正在识别」。改为：贴 FGS 后**先** `moveTaskToBack`，沉降约 300ms，再 hold ≥1s，然后进 Vision。

### 中号收支速览：跟槽渲图（ADR-062）

废止固定 364×182 / 宽度分桶；渲图宽高跟桌面槽位走，纵向按 `10:162:10` 画出透明边；`fitCenter`，不裁透明边。

### 程序日志：去掉临时排障面（ADR-065）

删 Dart `ShareProgress` 时序日志与 Widget options 全量 dump / `onResume` diag；截图 `settleLog` 仅保留状态跃迁（开始等待 / 重置 / 取消 / 非候选 / 短观察结论 / 替换 / 门闩通过或失败 / 总时限 / 删原补扫）。原生 FGS / ShareRelay / 语音音频会话只留 `Log.e`（及无图 uri 的 `Log.w`），去掉成功路径 `Log.i`。

### 版本与文档

- `pubspec.yaml` → `0.4.3+10`
- `docs/development.md` / `docs/framework.md` / `CONTEXT.md`：ADR-062 / 063（回源 hold）/ 064 / 065；非候选过滤
- GitHub Release 附带 `arm64-v8a` 真机测试包（本机 `venv/APK` 另留 split-per-abi 全套）

### 不受影响

前台选图不走截图关联窗；稳定期 3s / 短观察 2s / 替换后只再走稳定期仍按 ADR-048 保留部分。识别 / 落库 / 结果通知语义不变。

---

## 0.4.2

**版本：** `0.4.2+9`  
**相对：** `0.4.1+8`  
**日期：** 2026-08-26

### 智谱语音测连修复（ADR-059）

语音测连曾用空 data 的极短 WAV，智谱 `glm-4-voice` 返回 `1210 No audio tokens extracted`，导致无法测通、也就绑不上「语音直接记账」。改为对齐 BeeCount：1 秒 16-bit PCM 静音；静音空响应仍算成功；请求里 `input_audio` 置于文本之前。

### 语音记账音频模式还原（ADR-060）

仅 Android：语音弹层结束时条件还原 `AudioManager` 模式（仍为我们留下的通信类才写回）；「重新说」不还原，避免二次开麦被打断。

### 记一笔：备注旁语音入口

新建备注层左侧增加语音（右侧仍为相机）；关层丢草稿后走与扇形相同的单卡确认流。标签次级层标题行「确认」仅关层。

### 服务商编辑：测通失败落盘去留

部分模型测通失败仍落盘后，改为弹窗「返回编辑 / 确认保存」（确认回服务商管理；遮罩/返回留下），不再只用 SnackBar。

### 收支速览热区（ADR-061）

去掉眼睛图标；**金额热区**改为金额隐私切换（小/中独立）。小号：今日支出大数字→隐私，其余→记支出。中号：支出/收入金额行→隐私；「+」→记支出；柱图→报表近 7 日；标签与垫片等其余浮卡区→明细。中号画布固定 364×182（`resizeMode=none`），内容浮卡 364×162、上下透明各 10；柱图定高 76、距卡底 14。

### 分享入账早期进度（ADR-063）

热启分享后曾有 1–3s 无通知空窗（等 Dart 再起 FGS，且含固定 500/600ms delay）。改为 `ShareRelay` 拷图成功即起同 id FGS（「已收到，准备识别…」）；去掉 notify 固定 delay；Dart 接手 `startForegroundService`；**「已收到…」最短展示 1s**（`ShareEarlyProgressGate`）后再进 Vision 进度。

### 版本与文档

- `pubspec.yaml` → `0.4.2+9`
- `docs/development.md` / `docs/framework.md` / `CONTEXT.md`：ADR-059 / ADR-060 / ADR-061 / ADR-063
- `docs/adr/063-share-ingress-early-fgs.md`
- GitHub Release 附带 `arm64-v8a` 真机测试包（本机 `venv/APK` 另留 split-per-abi 全套）

### 不受影响

本机 schema、账单身份 / 指纹规则未变。0.4.1 前台弹层引导、多图/多选分享、多引擎语音等行为保持。识别 / 落库 / 结果通知语义不变。

---

## 0.4.1

**版本：** `0.4.1+8`  
**相对：** `0.4.0+7`  
**日期：** 2026-08-24

### 前台弹层未就绪引导（ADR-057）

语音/图片识别确认弹层在 AI 能力未就绪时，仅在弹层内展示「去设置」引导，不再叠加 SnackBar；后台直存失败通知仍独立展示。

### 多图选图与多选分享（ADR-058）

- 扇形「图片」/备注旁相册：系统多选（上限 9，超出截取并 Toast），串行 Vision 后按图分组确认；组头缩略图可点全屏原图；失败/空结果组可重试
- Android 分享入账：支持 `ACTION_SEND_MULTIPLE`，与后台直存同批处理；超出 9 张截取前 9 并注明

### 版本与文档

- `pubspec.yaml` → `0.4.1+8`
- `docs/development.md` / `docs/framework.md`：ADR-057 / ADR-058
- 本机 `venv/apk` 留 split-per-abi 全套

### 不受影响

本机 schema、账单身份 / 指纹规则未变。0.4.0 多引擎语音、Vision 回退、FGS 保活等行为保持。

---

## 0.4.0

**版本：** `0.4.0+7`  
**相对：** `0.3.2+6`  
**日期：** 2026-08-23

### 多引擎语音识别 + AI 语音直接记账（ADR-052）

语音扇形不再只有「听写→文本结构化」一条路径，可按引擎选择：

- **系统 ASR**（默认；不可用时置灰）
- **Vosk 中文小包** / **Whisper base**（ModelScope 下载，不进安装包）
- **AI 语音模型直接记账**（与文本/视觉并列第三项能力绑定）

AI 设置新增「语音直接记账」服务商绑定；旧配置无 `voiceModel` 时智谱补 `glm-4-voice`。确认门闩与单卡流程不变。

### Vision 服务商回退（ADR-055）

前台图片识别（扇形拍照/选图、识别确认弹层）在已测通服务商间自动切换：每商仅调 1 次，失败立即切下一候选；loading 展示失败原因与「正在切换至 XX（模型）重试…」。后台直存同样串行尝试，耗尽后按传输/API 区分通知标题（网络异常 / 识别失败）。

### 后台直存结果按账单笔数（ADR-056）

合并/单张结果通知的「成功 / 跳过 / 失败」按 **账单候选** 分桶（非图片张数、不含重试）；一张图内撞车跳过不中断后续候选。无候选的整图用「另有 N 张未入账」。点通知：成功笔 > 0 进明细，否则失败 Dialog。

### AI 生产请求日志（ADR-053）

每次 AI 生产调用（含 Vision 回退时的 3s 重试与同商切换）写入程序日志，tag `AI`（provider + model）；测连不打点。

### 版本与文档

- `pubspec.yaml` → `0.4.0+7`（新增 `record` / `whisper_ggml` / `vosk_flutter` 等语音依赖）
- `docs/development.md` / `docs/framework.md`：ADR-052 / ADR-053 / ADR-055 / ADR-056
- 本机 `venv/APK` 留 split-per-abi 全套

### 不受影响

本机 schema、账单身份 / 指纹规则未变。0.3.2 后台直存 FGS 与传输失败重试（ADR-054）行为保持。

---

## 0.3.2

**版本：** `0.3.2+6`  
**相对：** `0.3.1+5`  
**日期：** 2026-08-23

### 后台直存：前台服务 + 传输失败重试（ADR-054）

修复截图自动在纯后台发起 Vision 时高概率 `Failed host lookup`，以及冷启动分享入账后流程「休眠」、回前台才继续的问题。

- Android：`AutoBillingForegroundService`（`dataSync`）包裹 `AutoBillingService` 整批 Vision；进度通知与 `piggy_auto_billing` 同渠道 id 1001
- 分享入账：先拉起 FGS，再 `moveTaskToBack`
- 传输失败且当时 App 不在前台：入本机待重试队列，通知「打开 App 后将自动重试」；`resumed` 时自动串行重跑

### 版本与文档

- `pubspec.yaml` → `0.3.2+6`
- `docs/development.md` / `docs/framework.md`：ADR-054

## 0.3.1

**版本：** `0.3.1+5`  
**相对：** `0.3.0+4`  
**日期：** 2026-08-21

### 明细 / 我的 · 视觉微调

- 明细一体顶栏：账本字号加大、贴近猪标；右侧四钮更紧凑；记一笔上移（`bottom: 36`）
- 「我的」导航行：压低行高与标题/副标题行距；开关行不变；去掉 `dense` 以免字号被连带缩小

### 版本与文档

- `pubspec.yaml` → `0.3.1+5`

## 0.3.0

**版本：** `0.3.0+4`  
**相对：** `0.2.1+3`  
**日期：** 2026-08-21

### 下线 AI 智能助手（ADR-049）

移除报表浮动球、对话页与「我的 → AI 智能助手」开关。扇形拍照/图片、语音、截图自动、分享入账、CSV「AI 智能映射」与 AI 服务商配置保留。历史 `source = ai_chat` 账单不迁移；落库须显式传 `source`。

### 待核对账单（ADR-050）

后台直存（截图自动 / 分享）成功后进入待核对：明细行蓝框高亮（仅本次前台，进后台清除、回前台重打）；顶栏信封+红点+底部弹层（持久，一键已读）；点高亮行即已看；成功通知可滚到该笔。

### 记一笔时间选择（ADR-051）

弃用系统 `showTimePicker` 表盘。改为居中 Dialog：时/分双输入（始终两位）+ 小时 0–23 一屏网格 + 分钟每 5 分快捷；非 5 倍分钟原样显示；匹配档高亮；非法确定红字不关；写回仍保留原秒。

### 截图自动：稳定期 + 替换关联窗（ADR-045 / ADR-048）

Android 截图后：**稳定期 3s**（自该路径最近变更，同路径编辑覆盖会重置）+ **替换关联窗 15s**（自首次检测）。系统预览「另存新文件名并删原图」在关联窗内用短观察 **2s** 判定替换或连拍；替换后新文件只再等 3s 稳定期。稳定期满可给早期进度，**真正识别/落库**等到入账门闩（关联窗满或替换/连拍已决）。预览删除且无后继则取消。稳定期/门闩前不记「已处理」。连拍按路径独立。分享入账不受稳定期/关联窗影响。

### 分享入账：返回截图编辑不中断识别

分享图片入账后尽快 `moveTaskToBack` 回到来源（如系统截图编辑）；后台直存批次进行中，系统返回键改为送后台而非 `finish` MainActivity，避免 Flutter 引擎被拆掉后 Vision 静默停在「开始识别」无结果。截图自动同一套返回保活。

### 版本与文档

- `pubspec.yaml` → `0.3.0+4`
- `docs/development.md` / `docs/framework.md` / README / 使用教程：下线助手、待核对、时间选择、截图关联窗与分享返回保活
- 程序日志 tag `Screenshot` 增补：短观察 / 替换 / 进度 / 门闩
- 分享入账 / 后台直存：`AndroidActivityBridge` + MainActivity `retainOnBack`
- GitHub Release 附带 `arm64-v8a` 真机测试包（本机 `venv/APK` 另留 split-per-abi 全套）

### 不受影响

本机 schema、账单身份 / 指纹规则未变。程序日志、报表墓碑过滤、AI 分类消歧、小组件紧凑金额等仍按 0.2.1 行为。

---

## 0.2.1

**版本：** `0.2.1+3`  
**相对：** `0.2.0+2`  
**日期：** 2026-08-20

### 截图自动：稳定期（ADR-045）

Android 检测到截图后先进入约 **2s** 稳定期（自最近一次 MediaStore 变更起算）；系统预览里编辑/覆盖会重置计时，期满且文件仍在才识别入账。稳定期结束前不记「已处理」，避免裁剪后的同路径覆盖被跳过。预览里删除则取消本次。连拍按路径独立计时。分享入账不受影响。

### 程序日志：稳定期可见 + 条数上限

- 稳定期关键节点（开始等待 / 变动重置 / 删除取消 / 静止入账）经原生桥接到程序日志，tag 为 `Screenshot`，可在「关于 → 程序日志」查看与导出
- 程序日志条数上限 **1000 → 2000**（仍约 48 小时裁剪）

### 报表统计：排除墓碑（ADR-046）

报表 / 小组件 / AI 摘要的合计与构成此前未过滤 `deletedAt`，会把已删账单算进去，导致比明细偏大、构成笔数与下钻列表不一致。现与明细一致：只计存活账单。删除确认补充「删除后不可恢复」（单删 / 批量）。

### AI 分类消歧（ADR-047）

Prompt 分类列表改为按主类分组，嵌入主类裸名与「主类-子类」；说明先定主类、再选子类、都不贴则用主类。落库与确认弹层优先解析复合名，仍接受裸名；明细等展示逻辑不变（仍显示分类裸名）。

### 小组件：小号本月金额紧凑展示

小号「收支速览」底栏本月支出 / 收入过长易截断；改为紧凑格式：`<1k` 为 `¥`+整数，`[1k,1w)` 为 `¥x.xxk`，`≥1w` 为 `¥x.xxw`（档内截断两位）。今日支出仍完整两位小数。

### 版本与文档

- `pubspec.yaml` → `0.2.1+3`
- `CONTEXT.md` / `docs/development.md` / `docs/framework.md`：截图稳定期（ADR-045）；用户可见统计（ADR-046）；AI 分类消歧（ADR-047）；小号本月紧凑金额

### 不受影响

本机 schema、账单身份 / 指纹规则、分享入账路径未变。

---

## 0.2.0

**版本：** `0.2.0+2`  
**相对：** `0.1.0+1`  
**日期：** 2026-08-20

### AI 提取：账单时间消歧

截图 / 订单页常同时出现多个时间（如瑞幸：自取时间靠上、订单时间靠下）。旧 prompt 只写「优先支付时间」，遇到无「支付」字样的订单详情时，模型容易误取自取、配送等履约时间。

本版约定：

- **账单时间** = 资金变动完成时刻
- 优先级：支付 / 交易成功 / 付款 → 订单 / 下单 / 成交
- **禁止**当作账单时间：自取、配送、预约、预计送达、取餐等
- 若只有履约类时间 → 使用提取时刻
- 文本 / 语音 / 图片共用同一规则

账单指纹仍按「时间到秒」计算；提错时间会换指纹，本机若已有同额同时则无法再记。已入账的跨设备身份不随改时间而变。

### 同步：账单身份与指纹分离

跨设备认亲改为创建时 UUID（账单身份）；账本 + 金额到分 + 时间到秒只作指纹，用于本机去重和同步预览里的疑似重复。

- 改金额 / 时间不再裂成两条
- 两台各记一笔同额同时：预览可选 **保留** 或 **合并**（合并留下较晚改动，另一条墓碑）
- 账单墓碑超过 90 天：写云前裁掉，本机同步时硬删
- 工作区 JSON `version` → 2（账单带 `syncId`）；CSV 仍不带身份（导入即新身份）

### 后台直存：重复账单通知文案

截图自动 / 分享入账落库时，若命中指纹冲突（已存在相同账本、金额与时间的账单）：

- 通知栏标题：**记账取消**（不再用「识别失败」）
- 点失败通知后的 Dialog 标题：同步为 **记账取消**

其它失败路径标题不变。

### 分享入账：热启动不再闪启动页

Android 系统分享经透明 `ShareRelayActivity` 转发后再进入主界面。App 仍在前台时分享入账，不再闪一下品牌启动页。

### 桌面小组件

**进入体验：** 点小组件经 `WidgetRelayActivity` 中转；冷启动、热启动都不展示启动页（冷启可接受短暂浅色等待）。桌面图标冷启仍为蓝底白猪。

**眼睛隐藏：** 切换后就地重渲，避免金额先闪再变成 `****`。

**样式：**

- 小号：底栏「本月支出 / 本月收入」标签缩小一号
- 中号：渲图画布固定 364×182（不可拉伸）；内容浮卡 364×162，上下透明各 10；柱图定高 76、距卡底 14

### 版本与文档

- `pubspec.yaml` → `0.2.0+2`（关于页经 `package_info_plus` 自动显示）
- `docs/framework.md`、`docs/development.md`：账单身份 / 指纹 / 折合 / 墓碑裁剪；小组件布局与启动路径

### 不受影响

本机 schema 仍为 v9（`syncId` 列已存在，无需迁移）。分类 / 标签规则未变。工作区文件升级为 version 2。
