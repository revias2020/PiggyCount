# 标签组：类型化选标维度

标签从扁平列表升级为**恰好属于一个标签组**；组同时服务管理组织与智能选标。组分为**字符串组**（语义相关，组内可多选，智能最多 2 个）与**数值组**（金额落 `[min,max)` 区间，组内互斥至多 1 个，禁重叠）。自动生成标签仅当给出已有字符串组名 + 新标签名时才建标；不自动建组、不在数值组建档。旧标签迁入「默认」字符串组；有标签则禁删组；报表标签构成本轮仍扁平。

否决：仅文件夹式分组、未分组标签、随「全库组数量」切换配额、跨组同名、本轮做标签组构成下钻。
