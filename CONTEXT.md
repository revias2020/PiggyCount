# 小猪记账

个人记账 App 的领域用语。实现细节（Flutter / Drift / Token 数值）不写在此。

## Language

**视觉打磨**:
在既定主视觉（参考图气质 + 亮色蓝主色）下，补齐层次、留白、按压反馈与页面一致性，使界面「神似」参考图而不抠像素。
_Avoid_: 换皮, Material3 迁移（原生 Android 意义上的）, 美化清单照搬

**换皮**:
更换主色板、引入暗色模式、或放弃参考图气质另立一套视觉语言。
_Avoid_: 视觉打磨, 微调

**分类管理**:
按支出/收入两套维护**两层分类树**（主分类 / 子分类）；主列表只展示主分类；点主分类打开详情弹层可编辑、改为子类、删除、并管理其子分类。无「一级/二级记账模式」全局开关。
_Avoid_: 分类设置（易与 AI/云同步等设置页混淆）, 扁平单层分类（已否决）

**分类自定义图标**:
编辑分类时在图标网格**首位**虚线框「＋」从相册上传；强制 1:1 裁剪后存本地；**仅绑定当前分类**（非共享图库）。分类导出为 zip（清单 + `custom_icons/`）。
_Avoid_: 跨分类表情图库, 社区图标, 拍照上传

**主分类**:
`parentId == null` 的分类；出现在分类管理网格与记一笔第一层。可直接被账单引用；其下可挂零或多条子分类。有子分类时图标角标「…」。
_Avoid_: 一级分类（旧文档用语，易与「一级模式」混淆）

**子分类**:
挂在某个主分类下的分类（`parentId` 指向主分类）；不可再挂子类（深度上限 2）。记一笔时在点中的主分类下方展开区选择。
_Avoid_: 二级分类（旧「二级模式」用语）

**改为子类**:
将某个**无子分类**的主分类降为另一主分类下的子分类；若其下已有子分类则拒绝并提示「该分类下存在子分类，无法调整」。
_Avoid_: 无限嵌套挪动

**改为主分类**:
将子分类提升为顶层主分类（清空 `parentId`）。
_Avoid_: 改为子类

**移至其他主分类**:
将子分类改挂到另一个主分类下（仍保持为子分类）。
_Avoid_: 改为主分类

**分类删除规则**:
子分类有账单则禁删并提示；主分类若**自身**或**任一子分类**有账单则禁删（子类有账单时列出那些子分类名），否则级联删除主分类及其全部子分类。
_Avoid_: 静默清空账单分类而不提示

**分类自定义图标**:
编辑分类时在图标网格**首位**虚线框「＋」从相册上传；强制 1:1 裁剪后存本地；**仅绑定当前分类**（非共享图库）。分类导出为 zip（清单 + `custom_icons/`）。
_Avoid_: 跨分类表情图库, 社区图标, 拍照上传

**选择主分类弹层**:
「改为子类 / 移至其他主分类」时弹出主分类网格；未选中项灰显，选中项恢复彩色；副文案「修改后支持再次调整」；底部「确认」。
_Avoid_: 下拉列表式选择

**管理页感**:
系统 ListTile 堆叠出的设置页气质：层次弱、图标弱、操作挤在行尾、缩进像偏好设置、整体又闷又重。视觉打磨要消掉的反面样板。
_Avoid_: 设置页（「我的」里的配置入口可以偏列表，但不等于分类/标签也要同款）

**记一笔扇形菜单**:
明细页右下角「记一笔」**长按**弹出 Speed Dial：拍照 / 语音 / 图片；以 FAB **中心**为圆心、足够大半半径的左上 1/4 真扇形展开，钮与锚点净距清晰、互不重叠；展开时主钮**收成小圆锚点**；半透明遮罩 + 三钮短文案；**按住拖到目标松手**执行，未选中松手则取消。单击仍为手动记一笔。
_Avoid_: 长按上滑直接语音（已否决）, 中央底栏「+」扇形（我们无该壳层）, 参考图黄球配色, 相对右下角锚点的挤叠偏移, 展开后仍保留宽胶囊导致压钮

**拍照记账**:
扇形「拍照」→ 系统相机拍照 → Vision 识别确认入账。
_Avoid_: 记一笔弹层内嵌相机（手动记一笔仍无附图）

**图片记账**:
扇形「图片」→ 相册选图 → Vision 识别确认入账（承接原「我的 · 选择截图识别」）。
_Avoid_: 「我的」里再放选图入口

**AI 智能助手开关**:
「我的」智能区块中、排在「截图自动记账」**之前**；关闭后**仅隐藏报表页 AI 浮动球**；不影响记一笔扇形三项。
_Avoid_: 用该开关一键关掉语音/拍照/图片入口

**明细一体顶栏**:
仅「明细」Tab 使用参考图式顶区：品牌图标 +「小猪记账」+ 当前账本 ▾ + 日历入口 + 搜索入口；该 Tab **不**再显示壳层共用 `AppTopBar`。报表 / 我的仍用壳层顶栏。
_Avoid_: 三 Tab 强制同一顶栏（旧约定，已在明细侧否决）, 把日历/搜索塞进共用 AppBar, 纯文字品牌无图标

**月度汇总条**:
明细顶区下方：左侧点选年月（年在上、月在下 + ▾）；点开为**年月网格**底部弹层（选年到月后确认）；右侧三列 **收入｜支出｜结余**（结余 = 收入 − 支出）。本轮不做明细列表的支出/收入/全部类型筛选。
_Avoid_: 左右箭头切月为主交互, 只显示支出+收入两列, 系统 DatePicker 选月, 滚轮选月

**日历页**:
从明细顶栏进入的独立页；**打开时聚焦明细当前月**。月历格子有支出则 `-支出`、有收入则另起一行 `+收入`；点日看当日账单；「在该日记账」打开记一笔并**预填选中日**（未选则今天）；可跳转「今天」。日历聚焦月与明细当前月**互不回写**。日历页**不**放记一笔 Speed Dial FAB（入账靠「在该日记账」）。
_Avoid_: 底部弹层选日, 仅净额一格, 返回明细时强制同步月份, 打开日历永远落在系统当月, 日历页再挂一套 FAB

**列表标签位**:
日历/搜索等账单行右侧第二行，用**标签**占据参考图「账户/支付方式」的位置；无标签则留空。
_Avoid_: 虚构账户实体, 右侧只留金额永不展示标签

**标签**:
挂在账单上的独立标注，**不是**分类树的一层。每笔账单可挂多个标签，但须遵守其所属**标签组**的约束（见字符串组 / 数值组）。
_Avoid_: 分类, 子分类, 账户

**标签组**:
标签的归属容器：既用于标签管理里的组织，也作为智能记账时的**选标维度**。每个标签**恰好属于一个**标签组。
_Avoid_: 主分类（分类树）, 文件夹（仅 UI、无选标语义）, 未分组标签

**字符串组**:
一种标签组：组内标签是穷举的离散名称。智能选标按语义相关性从组内挑选；同一账单在组内**允许多选**。
_Avoid_: 数值组, 把字符串组当成金额分档

**数值组**:
一种标签组：组内每个标签带数值区间（如小额 / 大额）。智能选标按账单**金额落入哪个区间**挑选；同一账单在组内**互斥**（至多一个）。组内区间**禁止重叠**；金额落在空隙则该组不选。
_Avoid_: 字符串组, 用语义相关代替金额落区间, 重叠区间靠「最窄/优先级」消歧

**智能选标**:
智能记账为账单挑选标签的规则：每个**字符串组**最多选 2 个语义相关标签（可 0）；每个**数值组**按金额落区间最多选 1 个（可 0）。配额按组类型固定，不随「全库有几个组」变化。
_Avoid_: 单组取二、多组各一（已否决的随组数量切换）, 强制每组都选满

**实付金额**:
账单截图或文本同时出现应付与实付时，记账金额取优惠后的实付。
_Avoid_: 应付, 原价, 划线价

**提取备注**:
智能提取写入账单的备注：必须 ≤15 字；优先商家/店铺名、商品名、用户描述，多段用「-」连接并极简店名与长标题。
_Avoid_: 原文长标题整段照抄, 支付方式塞进备注（应用标签时）

**自动生成标签**:
开关开启时，智能记账仅当同时给出**已有字符串组的组名 + 新标签名**且组名匹配成功时，才在该组内创建标签；**不得**自动创建标签组，也**不得**在数值组内自动建档。组名对不上或未给组名则丢弃该新标签。关闭时只匹配已有标签。
_Avoid_: 自动新建标签组, 自动新建金额档, 无组归属的扁平新标签, 仅凭标签名启发式塞组

**标签组删除规则**:
组内仍有标签则禁删，须先将标签移出或删光；不级联删标签，也不把标签暗塞进「默认」组。
_Avoid_: 删组级联删标签, 删组时自动挪到默认组

**默认标签组**:
数据升级时为无组旧标签自动创建的字符串组（名称如「默认」），仅作迁移落点；之后用户可在标签管理中拆组、改名。
_Avoid_: 未分组标签（已否决）, 长期保留无组扁平兼容层

**标签名唯一**:
标签名称在全库唯一（不按组隔离）；不同组不得重名。展示需要消歧时可加组名前缀，但不靠跨组同名表达不同含义。
_Avoid_: 仅组内唯一（会导致搜索/列表/报表同名歧义）

**数值组区间**:
数值组内区间统一为左闭右开 `[min, max)`；最后一档可用无上界（`max` 为空）表示；禁止重叠故端点只属于一档。
_Avoid_: 双闭靠错开小数, 每条标签自选开闭

**标签构成（报表）**:
报表构成维度的「标签」仍按**标签名扁平聚合**；本轮不新增「按标签组」构成维度。
_Avoid_: 本轮就把标签组做成构成维度下钻

**空标签组**:
允许尚无标签的空组存在（建组占位）；智能选标跳过空组。
_Avoid_: 创建组时必须同时带首个标签

**账单搜索**:
独立搜索页（能力对齐 BeeCount）：关键词匹配备注 / 分类名 / 金额字串 / **标签名**；另有金额区间、日期区间、分类筛选；支持批量改备注、改分类、删除。作用域为**当前账本**。
_Avoid_: 仅当前月份内搜索, 无筛选的简易搜索, 关键词不含标签

**账单**:
一条记账记录（金额、分类、日期、备注、标签等），是账本里的事实数据。
_Avoid_: 明细（指 Tab/列表时）, 流水（口语）

**账单变更**:
对账单的任意增、删、改（含批量与导入产生的写入）。
_Avoid_: 明细更新（易被理解成「只刷新了明细列表 UI」）

**明细**:
底部「明细」Tab：按月浏览账单列表与月度汇总条。
_Avoid_: 账单（数据实体）, 把「账单变更」说成「明细更新」

**报表**:
底部「报表」Tab：在选定账本、周期与收支类型下的聚合视图（趋势、构成、对比、排行等）。
_Avoid_: 明细（列表）, 统计页（旧称）

**报表再显刷新**:
报表在「再次面对用户」时须反映此前全部账单变更：切到报表 Tab、从编辑/助手等回到报表，或报表 Tab 已可见时账单变更完成。不要求在后台（人看着别的 Tab）持续重算；有旧快照时静默换新，避免整页闪骨架。
_Avoid_: 报表实时监听（人在别的 Tab 也随每笔写入重算）, 仅换周期/账本才刷新（已否决的缺陷行为）, 再显时整页清空转圈

**胶囊切换条**:
云服务页与 AI 模型配置页上，用于互斥选项的胶囊分段控件（气质对齐 BeeCount CapsuleSwitcher）；不采用 Material `SegmentedButton`。云服务仍为一层三选一（关闭 / WebDAV / S3），AI 仍为二选一（智谱 / OpenAI 兼容）；本轮不引入 BeeCount 式两层模式或服务商列表体系。实现上抽本仓库精简共用组件（无箭头），选中态用品牌主色而非照搬黑底。
_Avoid_: SegmentedButton 换皮清单, 服务卡片式选商（本轮否决）, 离线/备份/云同步两层模式（本轮否决）, 原样移植 BeeCount 含箭头版, 两页各写一套互不复用

**云服务**:
「我的」里可选的 WebDAV / S3 入口：把当前账本做成 **CSV 快照** 上传或下载导入；默认关闭（仅本机）。配置与日常上传/下载同一功能域，但页面上配置可折叠，日常操作置顶。
_Avoid_: 云同步（易暗示实时双向同步）, 自动同步, 备份与同步（未作产品总名）

**数据管理**:
「我的」里对本机 **CSV 文件** 的导入/导出（分享到文件或从文件选入）；与云服务并列同卡，但不走远端。
_Avoid_: 云服务, 用「备份」单指本地 CSV

**CSV 快照**:
云服务上传/下载的载荷：当前账本导出的整份 CSV；下载后按现有导入规则**追加**入账，不静默清空本地。
_Avoid_: 增量同步包, 数据库文件直传, 静默覆盖本地

**连接测试**:
用**当前表单**（含未保存改动）里的凭证主动探测远端是否可用；**不是**「保存配置」的前置条件，也**不**把表单隐式保存。成功时可写入 **已测通** 标记（只记测通状态与连接指纹，不等于替用户点保存）。上传/下载是更重的业务操作，入口与测连分离，但成功上传/下载也可刷新已测通。入口文案「测试连接」；成功短提示，失败对话框。探测约定：WebDAV `OPTIONS`；S3 对象 URL 轻量 `HEAD`/`GET`；AI 配置页同一按钮内**先文本后视觉**，两项都过才算成功。同步方式为「关闭」时不展示测连入口。
_Avoid_: 保存时强制先测, 测连隐含保存整份表单, 取消独立「测试连接」入口改用上传代替探测, 静默测连不提示（手动测时）, 仅测文本模型, 测已落盘配置而忽略表单, AppBar 测连入口（本轮）, 内联色条状态（本轮）, 文本通过即整体成功

**已测通**:
某套连接指纹（同步方式 + 连接相关字段）曾被证明可用：来源为连接测试成功，或上传/下载成功。仅当配置**已保存**、字段齐、且当前指纹与已测通指纹一致时，才在「我的」露出快捷上传/下载，并在云服务页展示顶栏操作区。指纹不一致（改过凭证）则视为未测通，须再测或再成功传一次。
_Avoid_: 仅 kind≠none 就算可同步, 未保存草稿也当已测通, 保存配置时强制先测, 用「上次同步时间」代替测通标记（本轮不做）

**快捷同步**:
「我的 · 云服务」下方、仅在已测通时出现的「下载并导入 | 上传当前账本」双按钮；下载确认与云服务页相同，上传不额外确认。
_Avoid_: 行尾图标-only 同步, 把快捷同步做成独立设置项
